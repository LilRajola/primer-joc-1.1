# Trobar un fill
#$ + nom del fill

# Per declarar variables
# var variable
# per donar un tipus a la varible força := Vector2 (200,0)

# func _physics_process(delta: float) -> void:
#semblant a process però amb passos de física(constant)

# inputs del jugador
#func _input(event: InputEvent) -> void: (mira totel que fa el jugador)
#if.Input.is_action_pressed()

#assingnar tecles a noms d'accions
#projecte, project settings
